<style>
.hiw-section {
  padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
  padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
}

@media screen and (min-width: 750px) {
  .hiw-section {
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
  }
}

.hiw-container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 1rem;
}

.section-title {
  text-align: center;
  font-size: 2.5rem;
  font-weight: 700;
  margin-bottom: 1rem;
  color: rgb(var(--color-foreground));
  line-height: 1.2;
}

.section-subtitle {
  text-align: center;
  font-size: 1.1rem;
  color: rgba(var(--color-foreground), 0.75);
  margin-bottom: 3rem;
  max-width: 600px;
  margin-left: auto;
  margin-right: auto;
}

.hiw-steps {
  display: flex;
  gap: 2rem;
  justify-content: space-between;
}

.hiw-step {
  background: #fff;
  border-radius: 12px;
  padding: 2rem 1.5rem;
  text-align: center;
  flex: 1;
  box-shadow: 0 2px 12px rgba(0,0,0,0.08);
  transition: all 0.3s ease;
  cursor: pointer;
  border: 2px solid transparent;
  position: relative;
}

.hiw-step:hover {
  box-shadow: 0 8px 24px rgba(0,0,0,0.12);
  transform: translateY(-4px);
  border-color: #A3D9A5;
}

.hiw-icon {
  width: 80px;
  height: 80px;
  margin: 0 auto 1.5rem;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 48px;
}

.hiw-title {
  font-size: 1.2rem;
  font-weight: 600;
  margin-bottom: 0.75rem;
  color: rgb(var(--color-foreground));
}

.hiw-desc {
  color: rgba(var(--color-foreground), 0.75);
  font-size: 0.95rem;
  line-height: 1.5;
}

/* Mobile Styles */
@media (max-width: 768px) {
  .hiw-container {
    padding: 2rem 1rem;
  }
  
  .section-title {
    font-size: 2rem;
  }
  
  .hiw-steps {
    flex-direction: column;
    gap: 1rem;
  }
  
  .hiw-step {
    text-align: left;
    display: flex;
    align-items: flex-start;
    padding: 1.5rem;
    border-radius: 12px;
    position: relative;
  }
  
  .hiw-icon {
    width: 56px;
    height: 56px;
    margin: 0 1rem 0 0;
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 36px;
  }
  
  .hiw-content {
    flex: 1;
  }
  
  .hiw-title {
    margin-bottom: 0.5rem;
  }
  
  .hiw-desc {
    font-size: 0.9rem;
  }
  
  /* Removed - descriptions now always visible
  .hiw-step.collapsed .hiw-desc {
    display: none;
  } */
  
  /* Removed expand/collapse arrows - descriptions always visible
  .hiw-step::after {
    content: '▼';
    position: absolute;
    right: 1rem;
    top: 50%;
    transform: translateY(-50%);
    font-size: 0.8rem;
    color: rgba(var(--color-foreground), 0.4);
    transition: transform 0.2s;
  }
  
  .hiw-step.active::after {
    transform: translateY(-50%) rotate(180deg);
  } */
}
</style>

<section class="hiw-section color-{{ section.settings.color_scheme }}">
  <div class="hiw-container page-width">
  {% if section.settings.heading != blank %}
    <h2 class="section-title">{{ section.settings.heading }}</h2>
  {% endif %}
  {% if section.settings.subheading != blank %}
    <p class="section-subtitle">{{ section.settings.subheading }}</p>
  {% endif %}
  
  <div class="hiw-steps">
    {% for block in section.blocks %}
      <div class="hiw-step">
        <span class="hiw-icon">
          {% if block.settings.icon_svg != blank %}
            {{ block.settings.icon_svg }}
          {% elsif block.settings.icon_image != blank %}
            <img src="{{ block.settings.icon_image | img_url: '80x80' }}" alt="" style="width:80px;height:80px;">
          {% endif %}
        </span>
        <div class="hiw-content">
          <div class="hiw-title">{{ block.settings.title }}</div>
          <div class="hiw-desc">{{ block.settings.desc }}</div>
        </div>
      </div>
    {% endfor %}
  </div>
  </div>
</section>
<script>
// Removed accordion behavior - descriptions always visible
(function() {
  // Steps are no longer clickable to expand/collapse
  function removeClickBehavior() {
    // Keep function for future use if needed
  }
  
  // Initialize mobile view - removed collapsed state
  function initMobileView() {
    if (window.innerWidth <= 768) {
      // Descriptions are now always visible
      document.querySelectorAll('.hiw-step').forEach(function(step) {
        step.classList.remove('collapsed');
      });
    } else {
      document.querySelectorAll('.hiw-step').forEach(function(step) {
        step.classList.remove('collapsed', 'active');
      });
    }
  }
  
  // Click handlers removed - descriptions always visible
  // No initialization needed - descriptions always visible
})();
</script>

{% schema %}
{
  "name": "How It Works",
  "tag": "section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Section Title",
      "default": "How It Works"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Section Subtitle",
      "default": "Create your custom Perkie Print in 4 simple steps"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 36
    }
  ],
  "blocks": [
    {
      "type": "step",
      "name": "Step",
      "settings": [
        {
          "type": "textarea",
          "id": "icon_svg",
          "label": "SVG Icon",
          "default": "<svg viewBox='0 0 48 48' fill='none' xmlns='http://www.w3.org/2000/svg'><rect x='20' y='10' width='8' height='28' rx='4' fill='#A3D9A5'/><rect x='14' y='28' width='20' height='8' rx='4' fill='#F7C873'/></svg>"
        },
        {
          "type": "image_picker",
          "id": "icon_image",
          "label": "Or upload an icon image (overrides SVG if set)"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Step Title"
        },
        {
          "type": "textarea",
          "id": "desc",
          "label": "Description",
          "default": "Step description goes here."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "How It Works",
      "settings": {
        "heading": "How It Works",
        "subheading": "Create your custom Perkie Print in 4 simple steps",
        "color_scheme": "scheme-1",
        "padding_top": 36,
        "padding_bottom": 36
      },
      "blocks": [
        {
          "type": "step",
          "settings": {
            "icon_svg": "<svg viewBox='0 0 48 48' fill='none' xmlns='http://www.w3.org/2000/svg'><rect x='20' y='10' width='8' height='28' rx='4' fill='#A3D9A5'/><rect x='14' y='28' width='20' height='8' rx='4' fill='#F7C873'/></svg>",
            "title": "Select Your Product",
            "desc": "Choose from a variety of print styles and sizes."
          }
        },
        {
          "type": "step",
          "settings": {
            "icon_svg": "<svg viewBox='0 0 48 48' fill='none' xmlns='http://www.w3.org/2000/svg'><ellipse cx='24' cy='38' rx='12' ry='6' fill='#F7C873'/><rect x='18' y='10' width='12' height='20' rx='6' fill='#A3D9A5'/></svg>",
            "title": "Upload Your Image",
            "desc": "Upload your favorite photo to be transformed."
          }
        },
        {
          "type": "step",
          "settings": {
            "icon_svg": "<svg viewBox='0 0 48 48' fill='none' xmlns='http://www.w3.org/2000/svg'><circle cx='24' cy='24' r='22' fill='#E6E6E6'/><path d='M16 24l6 6 10-10' stroke='#A3D9A5' stroke-width='3' stroke-linecap='round' stroke-linejoin='round'/></svg>",
            "title": "Approve Your Artwork",
            "desc": "Review and approve the digital artwork before printing."
          }
        },
        {
          "type": "step",
          "settings": {
            "icon_svg": "<svg viewBox='0 0 48 48' fill='none' xmlns='http://www.w3.org/2000/svg'><rect x='6' y='28' width='28' height='10' rx='2' fill='#F7C873'/><rect x='34' y='32' width='8' height='6' rx='2' fill='#A3D9A5'/><circle cx='14' cy='40' r='3' fill='#A3D9A5'/><circle cx='38' cy='40' r='3' fill='#A3D9A5'/></svg>",
            "title": "We Create & Ship",
            "desc": "Your custom art is created and shipped directly to you."
          }
        }
      ]
    }
  ]
}
{% endschema %} 