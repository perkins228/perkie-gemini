# Secrets and credentials - NEVER COMMIT THESE
perkieprints-sa-key.json
.env
.env.*
*.key
*.pem
*-key.json
*_key.json
secrets/
# API keys pattern (Google, AWS, etc.)
*api*key*
*apikey*

# Dependencies
node_modules/

# Temporary log files (from Cloud Run debugging)
logs_*.json
temp_logs*.json
*_logs_*.json

# Test data files (base64, temp images)
*_base64.txt
temp_*.txt
test-*.txt
test-*.svg

# Python test scripts (backend belongs in backend/)
create_test_*.py

# Screenshot/debug images in root (allow only in asset directories)
/*.png
/*.jpg
!assets/**/*.png
!assets/**/*.jpg
!sections/**/*.png
!sections/**/*.jpg

# ROI/analysis text files
*_roi_*.txt
*_calculation*.txt

# Empty or malformed files
_ul
c:Users*

# Claude context sessions - keep only the active one
.claude/tasks/context_session_*.md
!.claude/tasks/context_session_001.md
!.claude/tasks/context_session_template.md
.claude/tasks/archived/
